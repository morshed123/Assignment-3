/*  
function budgetCalculator(watch,mobile,laptop) {
    var sum = 0;
    for (var i = 0; i <(watch,mobile,laptop).length; i++){
     var element = (watch,mobile,laptop)[i];
    sum = sum + element;
  }
  return sum;
 }

 var watch,mobile,laptop=[50,100,500];
 var result = budgetCalculator(watch,mobile,laptop);
 console.log("Total of the Budget:", result,"Taka");



function kilometerToMeter (kilometer){   

    var meter = (kilometer*1000);
     return meter;
    
}
  var meter = kilometerToMeter(10);
  console.log("Convert kilometer to meter =", meter ,"m");  
  




function megaFriend(array) {
  var name = array[0];
  for (i = 0; i < array.length; i++) {
    if (array[i].length > name.length) {
      name = array[i];
    }
  }
  return name;
}
var char =["Morshed", "Afsana", "Mimi", "Khanom", "Mister"];
console.log(megaFriend(char));

 */



 